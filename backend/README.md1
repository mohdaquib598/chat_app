# Awesome Project Build with TypeORM

Steps to run this project:

<!-- 1. Run `npm i` command
2. Setup database settings inside `data-source.ts` file
3. Run `npm start` command -->
1. Create a .env file  and add varaible with value
    MARIADB_HOSTNAME = db
    MARIADB_USER=
    MARIADB_PASSWORD=
    MARIADB_DATABASE=
    MARIADB_ROOT_PASSWORD=
    MARIADB_PORT= 
    PORT=

1. Navigate terminal at chat backend folder
2. Run command ./start_docker.sh
